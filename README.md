
  # School Management System Setup

  This is a code bundle for School Management System Setup. The original project is available at https://www.figma.com/design/69Jek4kHVjb5omWjAHl5zb/School-Management-System-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  